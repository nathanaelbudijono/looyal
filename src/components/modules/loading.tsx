import { AiOutlineLoading } from "react-icons/ai";

const LoadingPage = () => {
  return (
    <main className="h-screen flex justify-center items-center">
      <AiOutlineLoading className="text-xl aniamte-spin" />
    </main>
  );
};

export default LoadingPage;
